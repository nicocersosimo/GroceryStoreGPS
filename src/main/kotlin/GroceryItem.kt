enum class GroceryItem(val storeSector: StoreSector) {
    FLOWERS(StoreSector.ENTRANCE),
    BREAD(StoreSector.PASTRY),
    BAGELS(StoreSector.PASTRY),
    HAM(StoreSector.DELI),
    TURKEY(StoreSector.DELI),
    PEPPERONI(StoreSector.DELI),
    FRESH_CUT_CHEESE(StoreSector.DELI),
    CHICKEN(StoreSector.MEAT),
    GROUND_BEEF(StoreSector.MEAT),
    APPLES(StoreSector.PRODUCE),
    BANANAS(StoreSector.PRODUCE),
    LETTUCE(StoreSector.PRODUCE),
    ONION(StoreSector.PRODUCE),
    BURRITOS(StoreSector.FROZEN),
    PIZZA(StoreSector.FROZEN),
    CHIPS(StoreSector.PANTRY),
    CEREAL(StoreSector.PANTRY),
    BAR_SOAP(StoreSector.MISCELLANEOUS),
    VITAMINS(StoreSector.MISCELLANEOUS),
    MILK(StoreSector.FROZEN),
    BUTTER(StoreSector.FROZEN),
    COCONUT_WATER(StoreSector.PANTRY),
    SNACKS(StoreSector.PANTRY),
    CHEESE_PROTEIN_PACKS(StoreSector.DELI),
    RICE_CAKES(StoreSector.PANTRY),
    FLOUR(StoreSector.PANTRY),
    SUGAR(StoreSector.PANTRY),
    BUNS(StoreSector.PASTRY),
    TOMATO(StoreSector.PRODUCE),
    AVOCADO(StoreSector.PRODUCE),
    ORANGES(StoreSector.PRODUCE),
    LEMONS(StoreSector.PRODUCE)
}
